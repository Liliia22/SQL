CREATE VIEW STUDENTS_DEP AS
SELECT 
    SLM.STUDENT_ID,
    DLM.DEPARTMENT_NAME,
    SLM.TEACHER_ID,
    SLM.START_DATE,
    SLM.FULL_NAME,
    SLM.SCHOLARSHIP
FROM HR.STUDENTS SLM
INNER JOIN HR.DEPARTMENTS DLM
ON SLM.DEPARTMENT_ID = DLM.DEPARTMENT_ID
ORDER BY SLM.STUDENT_ID
;
