SELECT 
    PR.PRODUCT_NAME,
    S.COUNT_SALES, 
    S.DT_OPERATIONS,
    S.EMPLOYEE_ID,
    S.PRODUCT_ID,
    S.SUM_SALES 
FROM HR.PRODUCTS PR
LEFT JOIN HR.SALES S
ON PR.PRODUCT_ID = S.PRODUCT_ID
ORDER BY PR.PRODUCT_ID
;
